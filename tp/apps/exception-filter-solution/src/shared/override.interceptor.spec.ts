import { OverrideInterceptor } from './override.interceptor';

describe('OverrideInterceptor', () => {
  it('should be defined', () => {
    expect(new OverrideInterceptor()).toBeDefined();
  });
});
